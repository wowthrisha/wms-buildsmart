# Fuzzy scoring utilities — called by tn_engine.py
# _MIN_THRESHOLDS, _MAX_THRESHOLDS, _LEGACY_LABELS, _legacy_to_engine_fields,
# evaluate_rules were removed in Phase 3 (dead engine).
# All compliance scoring now goes through tn_engine.check_compliance()
